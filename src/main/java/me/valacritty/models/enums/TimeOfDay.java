package me.valacritty.models.enums;

public enum TimeOfDay {
    EARLY_MORNING,
    MORNING,
    EARLY_AFTERNOON,
    AFTERNOON,
    EVENING,
    IRRELEVANT, // TODO I don't like this enum but it works for now I guess
}
